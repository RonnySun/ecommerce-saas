# 秒算SaaS 客户手册

## 1. 项目全面介绍
秒算是一个“数据系统 + 大模型 Agent + 飞书协同”的电商经营平台。

一句话定位：\n把分散数据变成可问、可分析、可执行的经营决策。

核心卖点：
1. 多店铺数据统一看板：GMV、利润、ROI 一屏可见。
2. AI 智能导入：上传 Excel/CSV 后，大模型自动识别列名与字段映射。
3. AI 经营 Agent：对接大模型，支持自然语言分析与建议生成。
4. 飞书直连：在飞书里直接提问，直接收到业务结论。

---

## 2. Agent 关键介绍（重点）
秒算 Agent 是系统的核心能力，已接入大模型（MiniMax M2.5，Anthropic 兼容接口）。

它不是“聊天机器人”，而是“经营分析执行引擎”：
1. 识别问题意图：自动判断你问的是总览、店铺、财务还是预警。
2. 自动取数：调用后端经营数据工具，不靠人工拼报表。
3. 结构化回答：先结论，再数据，再建议，面向业务决策。
4. 双入口一致：网页对话和飞书对话能力一致。

Agent 调用链路：
1. 用户提问（Web 或飞书）
2. Agent 理解问题并触发数据工具
3. 大模型组织为业务结论
4. 返回可执行建议

---

## 3. 飞书联动介绍（重点）
系统已支持飞书机器人接入，业务团队无需切到后台页面。

你可以在飞书直接问：
1. 近7天店铺 GMV 怎么样？
2. 哪个店 ROI 最差？
3. 今天有异常预警吗？

飞书链路：
1. 飞书消息进入秒算监听器
2. 秒算 Agent 调用大模型与数据接口
3. 结果回写飞书会话

价值：管理层在飞书里就能拿到实时经营结论。

---

## 4. 页面功能说明
### 4.1 概览页
查看近 7/30/90 天核心指标：GMV、净利润、订单量、ROI。

### 4.2 财务页
查看成本拆分：广告费、平台佣金、退款、净利润。

### 4.3 店铺页
查看各店铺排行、平台表现对比。

### 4.4 导入数据页
上传 Excel/CSV，AI 自动识别列名并导入。

### 4.5 秒算（Agent）
直接输入问题，例如：
- 近7天哪个店 ROI 最低？
- 利润率下降原因是什么？
- 给我一份运营建议。

### 4.6 秒算配置
配置 4 个 MD 文件：`SOUL`、`SKILLS`、`CONTEXT`、`FEISHU`。

---

## 5. 文件导入（重点）

### 5.0 AI 导入引擎卖点（重点）
文件导入不是“纯模板导入”，而是“大模型增强导入”：
1. 大模型自动识别列语义（比如“销售额/成交金额”都会匹配到 GMV）。
2. 自动给出字段映射建议，减少人工配置时间。
3. 支持手动微调映射，兼容不同客户的历史表结构。
4. 导入后可立刻被 Agent 查询与分析，形成数据闭环。

### 5.1 支持格式
1. `.xlsx`
2. `.xls`
3. `.csv`

文件大小建议不超过 10MB。

### 5.2 导入步骤
1. 进入「导入数据」页面。
2. 拖拽文件或点击上传。
3. 系统自动分析列名并给出映射建议。
4. 检查每列映射（可手动改）。
5. 点击「确认导入」。
6. 查看导入结果（成功/失败行数）。

### 5.3 推荐列名（不强制）
- 订单号
- 店铺名称
- 销售额（GMV）
- 广告费
- 平台佣金
- 净利润（可选，不填会自动估算）
- 订单日期（必需）

### 5.4 导入规则
1. `订单日期` 必填。
2. 店铺不存在会自动创建。
3. 金额字段支持自动清洗（如 `¥1,999`）。
4. 导入失败会返回错误行号。

### 5.5 常见导入问题
1. 提示“缺少必要字段”：通常是没映射 `订单日期`。
2. 提示“文件已过期”：重新上传再确认。
3. 日期识别失败：改成标准日期格式（如 `2026-03-02`）。

---

## 6. 秒算配置（MD）

### 6.1 SOUL.md（角色与口吻）
定义 AI 如何说话、如何给建议。

### 6.2 SKILLS.md（行业规则）
写 ROI 基线、异常阈值、平台经验。

### 6.3 CONTEXT.md（企业背景）
写你公司品类、平台、近期目标。

### 6.4 FEISHU.md（飞书接入）
必须填写：
```md
App ID: cli_xxxxxxxxxxxxxxxx
App Secret: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```
填写后：
1. 点「保存」
2. 点「启动监听」
3. 到飞书发消息测试

---

## 7. 飞书接入步骤（重点）

### 7.1 飞书侧准备
1. 在飞书开放平台创建企业自建应用。
2. 开启机器人能力。
3. 获取 `App ID`、`App Secret`。
4. 给应用开通收发消息权限。

### 7.2 系统侧配置
1. 打开 `FEISHU.md`。
2. 填入 App ID / App Secret。
3. 保存并启动监听。
4. 状态显示“监听中”即可。

### 7.3 验证方法
1. 私聊机器人发：`测试123`
2. 群里发：`@机器人 店铺GMV多少`
3. 收到回复即成功。

---

## 8. 推荐提问模板（给业务同学）
1. 近7天整体经营情况怎么样？
2. 哪个店 ROI 最差？
3. 利润率为什么下降？
4. 给我本周投放优化建议。
5. 今天有没有异常预警？

---

## 9. 常见问题（FAQ）

### 9.1 飞书有时不回
1. 确认 FEISHU.md 已保存真实凭证。
2. 确认监听状态是“监听中”。
3. 先私聊测试，再测群聊。
4. 群聊请带正文，不要只 @。

### 9.2 看板没有数据
1. 先完成文件导入。
2. 确认导入结果有“成功行数”。
3. 检查导入的日期范围是否在查询区间内。

### 9.3 AI 回答不符合业务习惯
去「秒算配置」优化：
1. SOUL（语气）
2. SKILLS（规则）
3. CONTEXT（背景）

---

## 10. 运维联系人建议（交付时填写）
- 系统地址：`__________`
- 飞书负责人：`__________`
- 运营负责人：`__________`
- 技术支持联系人：`__________`
