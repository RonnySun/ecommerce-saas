# 秒算 Agent 客户使用手册（配置 MD + 飞书）

## 1. 手册用途
本手册用于指导客户完成两件事：
1. 配置秒算 Agent 的 4 个 MD 文件。
2. 接入飞书机器人并完成联调。

适用版本：`feature/vertical-agent` 分支部署版本。

---

## 2. 登录与入口
1. 打开你的秒算系统地址（例如 `http://服务器IP` 或你的域名）。
2. 登录后进入「秒算配置」页面。
3. 左侧可以看到 4 个配置文件：
- `SOUL.md`
- `SKILLS.md`
- `CONTEXT.md`
- `FEISHU.md`

---

## 3. MD 配置说明

### 3.1 SOUL.md（角色与语气）
作用：定义 AI 的身份、表达风格、行为边界。

建议内容：
1. 角色定位：例如“电商经营分析师”。
2. 回答原则：先结论再数据再建议。
3. 风格约束：简洁、可执行、不空话。

示例：
```md
# 角色定位
你是电商经营分析师，服务多平台店铺老板。

# 回答规则
- 先给结论，再给关键数据，再给动作建议
- 不确定时说明假设
- 避免冗长解释
```

### 3.2 SKILLS.md（行业知识）
作用：提供你的行业常识、指标基线、分析框架。

建议内容：
1. 平台 ROI 基准。
2. 常见异常阈值。
3. 季节性或大促规律。

示例：
```md
# ROI 基准
- 拼多多：1.5-2.5
- 天猫/京东：2.5-4.0

# 异常阈值
- GMV 环比下降 > 15% 触发预警
- ROI < 2.0 触发投放排查
```

### 3.3 CONTEXT.md（企业背景）
作用：告诉 AI 你的真实业务背景，避免“泛泛而谈”。

建议内容：
1. 公司信息与主营品类。
2. 主要平台与店铺。
3. 近期重点活动或目标。

示例：
```md
# 公司背景
主营个护小家电，客单价 129-299。

# 平台
天猫旗舰店、京东自营店、拼多多店。

# 当前目标
本月提升净利润率到 24%，降低退款率到 3% 以下。
```

### 3.4 FEISHU.md（飞书凭证）
作用：给系统提供飞书 App 凭证，建立消息收发链路。

必填项：
```md
App ID: cli_xxxxxxxxxxxxxxxx
App Secret: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
tenant_id: 1
```

注意：
1. 必须填真实值，不能保留示例占位符。
2. 修改后一定点击「保存」。
3. 保存后再点击「启动监听」。

---

## 4. 飞书接入步骤（客户版）

### 4.1 飞书开放平台准备
1. 进入飞书开放平台，创建企业自建应用。
2. 在「凭证与基础信息」获取：
- `App ID`
- `App Secret`
3. 在应用能力中启用机器人能力。
4. 给机器人开通需要的消息权限（收消息、发消息）。

### 4.2 系统内填写 FEISHU.md
1. 打开「秒算配置」的 `FEISHU.md`。
2. 填入 `App ID`、`App Secret`、`tenant_id`。
3. 点击「保存」。

### 4.3 启动监听
1. 在 FEISHU 页面点击「启动监听」。
2. 状态变为“监听中”表示连接成功。

### 4.4 验证收发
1. 在飞书私聊机器人发送：`测试123`。
2. 在群里 @机器人发送：`店铺GMV怎么样`。
3. 看到机器人回复即接入完成。

---

## 5. 客户常见问题与处理

### 5.1 能启动，但飞书不回复
排查顺序：
1. 确认 `FEISHU.md` 的 `App ID/App Secret` 是否真实。
2. 确认 FEISHU 页面状态是“监听中”。
3. 确认飞书应用权限已开通（收/发消息权限）。
4. 在群聊场景，建议先发“带正文”的 @ 消息，不要只 @。

### 5.2 有时回复，有时不回复
常见原因：
1. 刚改过 `FEISHU.md` 但未重新确认监听状态。
2. 群消息只有 @ 没有正文，导致可解析内容为空。
3. 飞书应用权限不完整，某类消息无法处理。

建议动作：
1. 重新保存 `FEISHU.md`。
2. 点击「停止监听」后再「启动监听」。
3. 用私聊 `测试123` 先验证链路。

### 5.3 页面显示运行中，但还是无回复
处理：
1. 先发一条私聊文本验证。
2. 让运维查看后端日志中是否有“收到消息/已回复”记录。
3. 若无入站日志，多数是飞书侧权限或配置问题。

---

## 6. 交付建议（给客户）
1. 首次上线后先做 3 条标准测试：私聊文本、群@文本、业务问题。
2. 每次改 `FEISHU.md` 后，都执行一次“停止监听 -> 启动监听 -> 发测试消息”。
3. `SOUL/SKILLS/CONTEXT` 建议每周小步迭代，不要一次大改。

---

## 7. 附录：推荐配置模板

```md
# SOUL
你是电商经营分析助手。
回答结构：结论 -> 数据 -> 建议。
语气：专业、简洁、可执行。

# SKILLS
平台ROI基准：拼多多1.5-2.5，天猫/京东2.5-4.0。
预警阈值：GMV环比下降>15%，ROI<2.0。

# CONTEXT
主营品类：____
主要平台：____
本月目标：____

# FEISHU
App ID: cli_xxx
App Secret: xxx
tenant_id: 1
```
